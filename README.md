# MRAG-Pipeline
A Multimodal Retrieval-Augmented Generation (MRAG) Pipeline for extracting and retrieving information from research documents such as PDFs. The pipeline processes text, tables, and images, stores embeddings in a vector database, and enables efficient retrieval for downstream AI applications.
